# Cloudflare Pages configuration
# Deploy with: wrangler pages deploy dist --project-name=shortcuts-generator

name = "shortcuts-generator"
compatibility_date = "2024-12-01"

[site]
bucket = "./dist"
